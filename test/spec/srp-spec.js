var srp = require('../../lib/srp');

describe('SRP Lib', () => {
  beforeEach((done) => {
     done();        
  });
  describe('srp_compute', () => {
      it('srp_compute_k', (done) => {
        done();
      });
  });
});
